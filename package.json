{
  "name": "ai-expense-tracker-fullstack",
  "private": true,
  "workspaces": [
    "backend",
    "frontend/ai-expense-tracker"
  ],
  "scripts": {
    "build": "cd frontend/ai-expense-tracker && npm install && npm run build"
  },
  "dependencies": {
    "serverless-http": "^3.0.0"
  }
}
